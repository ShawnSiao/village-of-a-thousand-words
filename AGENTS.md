# 《千字村》项目规则

## 语言边界

- 首轮版本仅支持中文。
- 所有面向玩家、测试者和设计审阅者展示的内容必须使用中文。
- 界面标题、按钮、提示、状态、角色属性、记忆标签、结局、试玩材料和设计文档不得直接展示英文内部标识符。
- 程序内部的变量名、字段名、数据键、文件名和其他机器可读标识符可以使用英文。
- 内部标识符需要出现在调试或实现资料中时，必须与中文名称明确区分，不得被界面直接读取并显示。
- 每个内部状态都应配置独立的中文显示名称，不得依赖程序对英文标识符进行临时翻译。

## 内容边界

- 不提供自由输入，不使用生成式人工智能续写剧情。
- 记忆文本、事件、对白和压缩版本全部由设计人员编写。
- 首轮版本围绕生存知识、人物关系和村庄信仰三套状态展开，不增加无关系统。

## MVP 范围

- MVP 共 15 回合，目标是完整交代千字村、千字碑、守字人和公共记忆的核心矛盾。
- 单局目标时长为 20～40 分钟，并支持多周目体验。
- 前 15 回合用于验证「保留、压缩、删除和改写记忆」是否同时产生策略选择与情感压力，不在 MVP 中扩展无关玩法。
- 世界可以在 MVP 后继续发展，但后续题材和事件不得破坏前 15 回合已经建立的规则与因果关系。

## 核心术语与世界规则

- 玩家身份统一称为「守字人」，负责听取证词、修改旧文和决定最终碑文。
- 执行雕刻和更换碑片的村民称为「刻字匠」；不得将「刻字匠」与玩家身份混用。
- 「守碑人」仅用于负责维护、清洁或看守石碑的角色，不承担碑文内容决策。
- 千字碑由固定碑身和可替换碑片组成。压缩和改写通过更换碑片完成，删除通过拆下并磨去原碑片文字完成。
- 被删除的碑文不得以完整、可公开查阅的档案继续存在，否则不构成真正的公共遗忘。
- 公共记忆与个人记忆必须明确区分。碑文被删除后，知情者可以暂时保留私人记忆，但后来者不能稳定继承这段内容。

## 红线托付

- 红线是「托付」的可交互标记，不得只作为装饰使用。
- 红线必须能够说明托付人、托付内容，以及哪些压缩、删除或改写会违背托付。
- 保留原意、压缩原意和违背原意必须呈现不同状态，并在人物反应或结局中产生可回溯的结果。

## 原型制作流程

- 凡是涉及新原型、原型改版或主要界面重设计，必须先使用 ImageGen 生成图片原型，再使用 Baoyu Design 绘制可交互前端原型。
- ImageGen 生成的背景图只负责氛围、构图和视觉方向，不承载必须阅读的标题、正文、按钮、规则或碑文。
- 每次原型迭代必须建立新的版本或目录，不得覆盖已经交付或用于对照的旧原型。
- 在视觉原型确认前，不直接对现有正式原型进行大范围样式重写。

## 可读性与界面底线

- 所有玩家必须阅读的文字必须使用真实文本层，不得将必要信息嵌入背景图片。
- 正文、对白和操作说明的字号不得小于 16 px；次要标签和页码不得小于 13 px；关键警告不得小于 16 px。
- 普通文字与背景的对比度不得低于 4.5:1，大号文字不得低于 3:1。
- 纹理背景必须为文字预留低纹理的安全区域，或在文字区域增加足够稳定的衬底、遮罩或渐变。
- 页面宽度增加时优先调整留白、栏数和内容区域，不得通过缩小字体容纳内容。
- 标题、正文和按钮不得出现影响阅读的孤字换行、过窄文本列或无意义断行。
- 容量超限属于主要操作状态，必须显示清晰的超限字数、下一步动作和可处理的旧记忆；超限时不得允许直接封存本年。

## 叙事与因果反馈

- 每次重要选择的结果必须同时包含人物或环境的叙事反应，以及清晰的规则解释。
- 规则解释必须明确写出「保留了什么」「删去了什么」「谁仍然记得」和「未来可能影响什么」。
- 不得使用「删去某人」等含义不完整的概括，必须说明删去的是姓名、行为、关系、知识、身份还是其他具体事实。
- 延迟后果必须能够回溯到对应回合、原始记忆和具体改写，不得只给出无法验证的文学暗示。
- 文学化对白可以保留，但不得替代玩家理解玩法所需的直接说明。
